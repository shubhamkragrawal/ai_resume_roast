# Core dependencies
streamlit>=1.28.0
pdfplumber>=0.10.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0
numpy>=1.24.0

# Google Gemini API
google-generativeai>=0.3.0

# Text-to-Speech options (install at least one)
# Option 1: Edge TTS (RECOMMENDED - best quality, realistic human voices)
edge-tts>=6.1.0

# Option 2: Google TTS (good quality, requires internet)
gTTS>=2.4.0

# Option 3: pyttsx3 (offline, lower quality)
pyttsx3>=2.90

# Optional: for better audio playback testing
# pygame>=2.5.0

# AI-based parsing enhancement (optional but recommended)
# This enables better section detection
# Already included via transformers

# Additional utilities
python-multipart>=0.0.6